//
// Created by Rostyslav Liapkin on 23.02.2025.
//

#include "Embedding.h"

/**
 * Constructor to generate random embedding layer
 * @param vocab_size vocabulary size
 * @param embedding_dim embedding dimension
 */
/*Embedding::Embedding(int vocab_size, int embedding_dim) : vocab_size(vocab_size), embedding_dim(embedding_dim) {
    // generate random matrix
    embedding_matrix = Eigen::MatrixXf::Random (vocab_size, embedding_dim);

}*/
